
0. Copy and paste these files into ~/ using VI (not vim)
    - 'config_ubuntu.sh'
    - 'clone_my_configs.sh'
    - '.install_vim.sh'
   (NOTE: make sure the file names are the same)
1. Change those files to executables (chmod 755 ___)
2. Then run the 'config_ubuntu.sh' script
3. While the script is running, open the 'open_me.sh' script --> it's a bunch of stuff that 
   has to be done manually

